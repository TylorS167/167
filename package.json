{
  "name": "167",
  "version": "0.39.0",
  "description": "Type-safe functional utilities",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "jsnext:main": "lib.es2015/index.js",
  "module": "lib.es2015/index.js",
  "repository": "https://github.com/TylorS167/167",
  "author": "Tylor Steinberger <tlsteinberger167@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@typed/either": "1.0.0",
    "@typed/future": "1.0.0",
    "@typed/maybe": "1.0.0"
  },
  "devDependencies": {
    "@typed/test": "2.5.0",
    "dox": "0.9.0",
    "glob-expand": "0.2.1",
    "husky": "0.14.3",
    "lint-staged": "4.1.2",
    "prettier": "1.6.1",
    "typescript": "2.5.2"
  },
  "lint-staged": {
    "*.ts": [
      "prettier --write --print-width 100 --tab-width 2 --no-semi --single-quote --trailing-comma es5 --parser typescript",
      "git add"
    ]
  },
  "scripts": {
    "build": "yarn build:commonjs && yarn build:es2015",
    "build:commonjs": "tsc -P .config/tsconfig.commonjs.json",
    "build:es2015": "tsc -P .config/tsconfig.es2015.json",
    "docs": "node tools/docs.js",
    "postversion": "yarn changelog && git push origin master --tags && npm login && npm publish --access=public && yarn docs && git add README.md && git commit -m 'generate API documentation' && git push origin master",
    "precommit": "lint-staged",
    "preversion": "yarn test && yarn build",
    "release:major": "npm version major -m 'chore(package): v%s'",
    "release:minor": "npm version minor -m 'chore(package): v%s'",
    "test:lint": "prettier --write --print-width 100 --tab-width 2 --no-semi --single-quote --trailing-comma es5 --parser typescript 'src/*.ts' 'src/**/*.ts'",
    "test": "typed-test 'src/*.test.ts' 'src/**/*.test.ts'"
  }
}
